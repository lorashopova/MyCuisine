{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    ".read": true,
    ".write": "auth != null",
    "meal" : {
      ".indexOn": ["category", "title"]
    }
  }
}