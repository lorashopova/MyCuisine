<h5 class="text-uppercase">Recent post</h5>
<div class="shadow">
    <ul class="list-unstyled">
        {{#each recentPosts}}
        <li><a href="#/meal/{{_id}}" title="meal">{{title}}</a></li>
        {{/each}}
    </ul>
</div>